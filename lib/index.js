/**
 * Collection extensions for Clay-Resource
 * @module clay-resource-collection
 */

'use strict'

const d = (module) => module && module.default || module

const define = d(require('./define'))

module.exports = {
  define
}
