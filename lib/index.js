/**
 * Collection extensions for Clay-Resource
 * @module clay-resource-collection
 */

'use strict'

const _d = (module) => module && module.default || module

const define = _d(require('./define'))

module.exports = {
  define
}
